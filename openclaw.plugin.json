{
  "id": "primeta",
  "kind": "channel",
  "channels": ["primeta"],
  "configSchema": {
    "type": "object",
    "properties": {
      "primeta": {
        "type": "object",
        "properties": {
          "enabled": { "type": "boolean" },
          "serverUrl": { "type": "string", "format": "uri" },
          "apiKey": { "type": "string" },
          "bridgeName": { "type": "string" }
        },
        "required": ["serverUrl", "apiKey"]
      }
    }
  }
}
